apiVersion: v1
kind: Service
metadata:
  name: {{ include "karma-mcp.fullname" . }}
  labels:
    {{- include "karma-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "karma-mcp.selectorLabels" . | nindent 4 }}