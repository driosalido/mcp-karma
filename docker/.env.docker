# Docker environment configuration for Karma MCP Server
# Copy this file to .env for docker-compose usage

# Karma Alert Dashboard URL
KARMA_URL=http://karma:8080

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional: Custom container name
# CONTAINER_NAME=karma-mcp-server

# Optional: Custom image tag
# IMAGE_TAG=latest